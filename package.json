{
    "name": "aigit-monorepo",
    "version": "1.0.0",
    "private": true,
    "description": "Root workspace to execute tests across sub-packages",
    "scripts": {
        "check:ledger": "node context-server/scripts/check-ledger-regression.mjs",
        "test": "npm test --prefix context-server"
    }
}
